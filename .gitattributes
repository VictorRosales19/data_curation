*.csv filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Exclude example zip files from LFS
RawDataExample/* !filter !diff !merge -text
CuratedDataExample/* !filter !diff !merge -text